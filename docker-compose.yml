version: '3.8'

services:
  myapp:
    build: .
    image: ins_comp
    expose:
     - "8000"
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - myapp
   